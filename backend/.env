PORT=5000
JWT_SECRET=festival-maintenance-super-secret-key-change-in-production
ADMIN_USERNAME=Admin
ADMIN_PASSWORD=Admin@123

# MySQL connection (see setup.sql to create the database and tables)
DB_HOST=localhost
DB_PORT=3309
DB_USER=root
DB_PASSWORD=
DB_NAME=festival_finance

# Comma-separated list of extra allowed CORS origins, if needed
# CORS_ORIGIN=https://example.com
